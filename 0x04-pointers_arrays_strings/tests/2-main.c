#include "dhk.h"
#include <stdio.h>

/**
 * main - check the code for DHK School students.
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;
	int len;

	str = "DHK!";
	len = _strlen(str);
	printf("%d\n", len);
	return (0);
}
